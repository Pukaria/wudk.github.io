# 深交所数据自动化稽核系统

## 📌 项目背景

在完成上交所数据自动化后，深交所数据的稽核工作仍依赖**人工每日登录官网、手动导出Excel、逐项比对**，效率低下且易出错。同时，深交所数据源具有**多接口、多格式、多频次**的特点（JSON API、HTML表格、实时行情接口等），对自动化系统的**健壮性和兼容性**提出了更高要求。

本系统作为**双交易所自动化体系**的深交所侧实现，实现了从**数据抓取 → 智能映射 → 数据库比对 → 钉钉通知**的全链路自动化，与上交所系统协同工作，共同构成完整的**市场数据质量保障体系**。

## 🛠️ 技术栈

| 类别 | 工具/技术 | 用途 |
|:---|:---|:---|
| **核心语言** | Python 3.x | 主开发语言 |
| **数据抓取** | Requests + Session（含重试机制） | HTTP API 数据获取（深交所 JSON 接口） |
| **HTML 解析** | BeautifulSoup4 | 辅助数据清洗与文本处理 |
| **数据处理** | Pandas, OpenPyXL | Excel 读写、数据清洗与转换 |
| **数据库** | PyODBC + SQL Server（CTE + 窗口函数） | 生产环境数据查询 |
| **通知系统** | 钉钉机器人 Webhook（加签模式） | 执行结果实时推送与异常告警 |
| **日期处理** | chinese_calendar | 交易日判断（剔除周末/节假日） |
| **HTTP 增强** | urllib3 Retry + 会话适配器 | 请求重试与容错 |
| **部署** | PyInstaller | 打包为独立 .exe 可执行文件 |

## 🧠 系统整体架构

本系统采用 **“四阶段流水线 + 多源并行抓取”** 架构：

```mermaid
flowchart TD
    subgraph A[阶段一：数据抓取（8个数据源）]
        A1[市场总貌<br>HTTP JSON] --> A2[日度成交量<br>HTTP JSON]
        A2 --> A3[基金日度概况<br>HTTP JSON]
        A3 --> A4[股票指标<br>HTTP JSON（3个市场）]
        A4 --> A5[指数行情<br>HTTP JSON（近一周）]
        A5 --> A6[融资融券<br>HTTP JSON]
        A6 --> A7[深港通汇率<br>分页循环抓取]
        A7 --> A8[实时指数<br>实时行情接口]
    end

    subgraph B[阶段二：智能映射]
        B1[读取模板文件] --> B2[别名映射表<br>（200+ 映射规则）]
        B2 --> B3[派生指标计算<br>（数据库求和）]
        B3 --> B4[忽略指标过滤]
        B4 --> B5[单位转换处理]
    end

    subgraph C[阶段三：数据库比对]
        C1[CTE + 窗口函数<br>按 指标×日期 查最新] --> C2[逐行比对]
        C2 --> C3[差异分级<br>完全一致/基本一致/存在偏差/差异较大/数据库无数据]
    end

    subgraph D[阶段四：钉钉通知]
        D1{存在异常?} -->|是| D2[@指定人员<br>推送问题报告 + 偏差示例]
        D1 -->|否| D3[推送成功通知]
    end

    A --> B --> C --> D
```

### 与上交所系统的对比

| 对比项 | 上交所系统 | 深交所系统 |
|:---|:---|:---|
| **数据接口类型** | Selenium（动态页面）+ JSONP | HTTP JSON（原生 REST） |
| **抓取方式** | 单日定点抓取 | **近一周范围抓取** |
| **映射方式** | 手动映射表 + 核心词匹配 | **别名映射表（200+）** + 派生指标计算 |
| **派生指标** | 融资偿还额、融券偿还量 | **7 个衍生求和指标**（从数据库实时计算） |
| **输出粒度** | 单日比对 | **多日全量比对**（含日期维度） |
| **PyInstaller 打包** | 支持 | **完整支持**（含资源文件嵌入） |

## 📥 阶段一：数据抓取（8 个数据源）

系统自动从以下 8 个数据源并行/串行抓取数据：

| 序号 | 数据源 | 接口类型 | 关键数据 |
|:---:|:---|:---|:---|
| 1 | 市场总貌 | HTTP JSON | 上市公司数、上市证券数、总市值、流通市值、成交金额（含各板块细分） |
| 2 | 日度成交量 | HTTP JSON | 主板/创业板/深市合计的成交量 |
| 3 | 基金日度概况 | HTTP JSON | ETF/LOF/公募REITs 成交量、成交金额 |
| 4 | 股票指标 | HTTP JSON（3 个 Tab） | 主板/创业板/深圳市场的：平均市盈率、平均换手率、总股本、流通股本等 |
| 5 | 指数行情 | HTTP JSON（按日期范围） | 近一周各指数的每日收盘价 |
| 6 | 融资融券 | HTTP JSON | 融资余额、融券余额、融资买入额、融券卖出量、融券余量 |
| 7 | 深港通汇率 | HTTP JSON（分页循环） | 参考汇率买入价、参考汇率卖出价 |
| 8 | 实时指数 | 实时行情接口 | 深证成指、深证综指、创业板指的实时价格 |

### 关键技术亮点

**1. 多日期范围抓取**

不同于上交所的单日抓取，深交所支持按日期范围批量抓取历史数据：

```python
def get_trading_dates(start_date: str, end_date: str) -> list:
    """获取指定日期范围内的所有交易日"""
    start = datetime.strptime(start_date, "%Y-%m-%d")
    end = datetime.strptime(end_date, "%Y-%m-%d")
    dates = []
    current = start
    while current <= end:
        if is_workday(current):
            dates.append(current.strftime("%Y-%m-%d"))
        current += timedelta(days=1)
    return dates
```

**2. 健壮的 Session 管理 + 重试机制**

使用 `urllib3.Retry` 实现自动重试，对 5xx 错误和超时进行容错：

```python
def create_session():
    session = requests.Session()
    retry = Retry(total=3, backoff_factor=1, status_forcelist=[500, 502, 503, 504])
    adapter = HTTPAdapter(max_retries=retry)
    session.mount("http://", adapter)
    session.mount("https://", adapter)
    return session
```

**3. 分页数据循环抓取（深港通）**

深港通汇率接口采用分页返回，系统自动循环翻页直到获取全部数据：

```python
def fetch_sgt_rate_history(session, start_date, end_date):
    page = 1
    while True:
        params = {'PAGENO': page, ...}
        data = session.get(url, params=params).json()
        if page >= total_pages:
            break
        page += 1
```

**4. 多 Tab 数据整合（股票指标）**

深交所股票指标页面包含“深圳市场”、“主板”、“创业板”三个 Tab，系统分别请求后合并：

```python
tabs = {'深圳市场': 'tab1', '主板': 'tab2', '创业板': 'tab4'}
for market, tabkey in tabs.items():
    params['TABKEY'] = tabkey
    # 抓取后添加市场后缀区分
    rows.append({'指标名': f"{name}_{market}", '数值': val})
```

## 🔄 阶段二：智能映射（含派生指标计算）

### 映射方式（4 层匹配策略）

| 层级 | 匹配方式 | 说明 | 示例 |
|:---:|:---|:---|:---|
| 1 | **精确匹配** | 模板名称与爬虫名称完全一致 | `融资余额` → `融资余额` |
| 2 | **别名映射表** | 200+ 预定义别名规则 | `深证成份指数:收盘价:日` → `深证成指_深圳市场` |
| 3 | **核心词清洗匹配** | 去除后缀词后进行匹配 | `股票总市值:深圳证券交易所:日` → `股票总市值` |
| 4 | **模糊匹配** | 双向包含关系 | `农林牧渔指数` ↔ `农林指数` |

### 派生指标计算

系统支持**从数据库实时计算派生指标**，当前配置了 7 个衍生求和指标：

```python
DERIVED_DEPENDENCIES = {
    '110264163': ['110263903', '110263948'],  # 衍生指标A = 指标1 + 指标2
    '110264164': ['110263902', '110263950'],  # 衍生指标B = 指标3 + 指标4
    # ... 共7组
}
```

当爬虫数据中找不到某个指标时，系统自动从数据库查询其依赖指标的同日数值并求和。

**派生计算逻辑**：

```python
def fetch_derived_value_from_db(zbdm, date_str, conn):
    dep_codes = DERIVED_DEPENDENCIES[zbdm]
    # 使用 CTE 查询依赖指标在指定日期的数值
    sql = """
    WITH AllData AS (
        SELECT ZBDM, JZRQ, ZBSJ FROM usrXHGSJBHG
        UNION ALL ...
    )
    SELECT ZBDM, ZBSJ FROM AllData
    WHERE ZBDM IN (?) AND JZRQ = ?
    """
    # 求和后返回
    total = sum(values)
    return total, dependency_description
```

### 单位转换与过滤

```python
# 忽略指标（不参与映射和比对）
IGNORE_ZBDM = ['110259675', '110259694', ...]  # 19个

# 单位转换（如：原始值 / 1,000,000）
UNIT_CONVERT_ZBDM = {'110263812': 1000000}
```

## 📊 阶段三：数据库比对（多日期维度）

### 比对逻辑

与上交所版本不同，深交所系统支持**按日期维度进行全量比对**：

1. **按 `ZBDM + JZRQ` 联合匹配**，查询数据库中该指标在**指定日期**的最新记录
2. 使用 **CTE + 窗口函数** 高效查询（`ROW_NUMBER() OVER (PARTITION BY ZBDM, JZRQ ORDER BY XGSJ DESC)`）
3. 逐行计算**绝对差异**和**相对差异**

```python
sql = """
WITH AllData AS (
    SELECT ZBDM, JZRQ, ZBSJ, XGSJ FROM usrXHGSJBHG
    UNION ALL ...
),
Ranked AS (
    SELECT ZBDM, JZRQ, ZBSJ,
        ROW_NUMBER() OVER (PARTITION BY ZBDM, JZRQ ORDER BY XGSJ DESC) AS rn
    FROM AllData
    WHERE ZBDM IN ({placeholders}) AND JZRQ IN ({date_placeholders})
)
SELECT ZBDM, JZRQ, ZBSJ FROM Ranked WHERE rn = 1
"""
```

### 比对状态分类

| 比对状态 | 判定条件 | 业务含义 |
|:---|:---|:---|
| **完全一致** | 绝对差异 = 0 | 数据完全匹配 ✅ |
| **基本一致** | 相对差异 < 0.01% | 差异微小，可接受 ✅ |
| **存在偏差** | 相对差异 0.01% ~ 1% | 需人工核对 ⚠️ |
| **差异较大** | 相对差异 ≥ 1% | 需重点核查 🔴 |
| **数据库无数据** | 数据库无该指标+日期记录 | 需新增录入 🔴 |

## 📱 阶段四：钉钉智能通知（含 LRMBMX 标识）

### 业务标识

系统在钉钉通知中引入 **`LRMBMX`**（新宏观-D1-深圳证券交易所）作为关键业务标识，便于群内人员快速识别消息所属业务线：

```python
LRMBMX = "新宏观-D1-深圳证券交易所"
```

### 通知内容结构

| 信息模块 | 内容 |
|:---|:---|
| **关键标识** | `LRMBMX`（业务线标识） |
| **数据时效** | 爬虫最新日期 vs 数据库最新日期 |
| **比对统计** | 完全一致/基本一致/存在偏差/数据库无数据的数量和占比 |
| **偏差示例** | 存在偏差时，展示前5条具体差异（含指标名、日期、数值对比） |
| **缺失示例** | 数据库无数据时，展示前5条缺失项 |
| **生成文件** | 比对结果 Excel 文件路径 |

### 通知逻辑

```python
has_issue = (need_update + missing) > 0
if has_issue:
    # 推送问题报告 + @指定人员
    notifier.send_markdown(title, content, at_mobiles=NOTIFY_MOBILES)
else:
    # 推送成功通知（不@人）
    notifier.send_markdown(title, content, at_mobiles=[])
```

## 📈 成果与价值

### 效率提升

| 对比项 | 人工操作 | 系统执行 | 提升幅度 |
|:---|:---:|:---:|:---|
| 每日稽核耗时 | 90-120 分钟 | **< 5 分钟** | **95%+** |
| 数据源覆盖 | 3-4 个（手工复制） | **8 个** | 覆盖翻倍 |
| 映射准确率 | 约 85%（人工疲劳） | **98%+** | 显著提升 |
| 跨日期比对能力 | 不支持（仅看当日） | **近一周全量** | 新增能力 |
| 异常实时告警 | 否 | **钉钉即时推送** | 新增能力 |

### 系统特性

- ✅ **全自动化**：从抓取到通知，全程无人值守
- ✅ **多日期范围抓取**：支持近一周交易日数据批量获取
- ✅ **200+ 别名映射表**：覆盖深交所全部核心指标
- ✅ **派生指标计算**：7 个衍生指标从数据库实时计算
- ✅ **单位自动转换**：支持自定义单位换算规则
- ✅ **多 Tab 数据整合**：自动合并深圳市场/主板/创业板数据
- ✅ **分页数据循环抓取**：深港通汇率支持全量历史数据
- ✅ **生产级数据库集成**：CTE + 窗口函数高效查询
- ✅ **钉钉实时通知**：含业务标识 `LRMBMX`，执行结果与异常告警即时推送
- ✅ **PyInstaller 打包部署**：完整支持资源文件嵌入

## 🔗 关联工具

本系统与上交所系统共同构成**双交易所数据质量保障体系**：

```text
[上交所官网] → [上交所自动化系统]
[深交所官网] → [深交所自动化系统] → [钉钉通知]
                                    ↓
                            [比对结果 Excel]
```

- 📊 [上交所自动化稽核系统](上交所数据自动化稽核系统.md) — 上交所侧实现
- 📊 [宏观数据自动比对工具](宏观数据自动比对工具.md) — 两期 Excel 数据比对
- 📊 [数据完整性核查工具](数据完整性核查与自动补全工具.md) — 按月检查数据连续性

## 📂 相关资源

- 📦 完整项目代码：[GitHub 仓库](https://github.com/Pukaria/python-scripts-collection/blob/main/深交所数据自动化稽核系统.py)

---

*系统状态：✅ 已投产使用，每日自动执行*
*作者：吴代奎（Wudk）*
*版本：v7.6*